services:
  server:
    build:
      context: .
    environment:
      NODE_ENV: production
      DB_PASSWORD: "YOUR_ACTUAL_MARIADB_ROOT_PASSWORD_HERE"
    ports:
      - "5000:5000"
    networks:
      - etodo_network

networks:
  etodo_network:
    driver: bridge